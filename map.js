//let arr1=[1,2,3,4,5];
/*
for(let i=0;i<arr1.length;i++){
    console.log(arr1[i]);
}
for(let ele in arr1){
    console.log(ele);  // This will print the index of the element in the array
}
for(let ele of arr1){
    console.log(ele);  // This will print the element itself
}*/
/*const a=arr1.forEach(function(ele){
    console.log(ele);  // This will print the element itself
    return ele;  // This will not return anything because forEach does not return a new array
});
/*arr1.forEach((ele)=>{
    console.log(ele);  // This will print the element itself
});*/
/*arr1.map((ele,index,arr)=>{
    console.log(ele>2);  // This will print the element itself
    return ele
  //  console.log(`array ${arr}  element ${ele} is at index ${index}`);  // This will print the element, its index and the array itself
});
*/
/*const xarr1=arr1.filter((ele)=>{
    return ele>2;  // This will return a new array with only the elements that are greater than 2
});
console.log(xarr1);  // [3, 4, 5] - This will print the new array that is returned by the filter method
*/
